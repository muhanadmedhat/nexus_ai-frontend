# Terraform — never commit state (holds secrets) or local cache
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
*.tfvars
*.tfvars.json
crash.log
crash.*.log

# Stray local artifacts
awscliv2.zip
aws/
